import { sharedAssetsAssets } from './shared-assets-assets';

describe('sharedAssetsAssets', () => {
    it('should work', () => {
        expect(sharedAssetsAssets()).toEqual('shared-assets-assets');
    });
});
